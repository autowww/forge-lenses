# PDCA 05 — Studio workshop editor

## Do

`BoardWorkshopEditor.tsx`: kanban HTML5 DnD, freeform pointer drag, add/edit/delete, autosave ~650ms. Wire in `BoardEditorPage.tsx`.

## Check

`cd lenses-enterprise && npm run build`
