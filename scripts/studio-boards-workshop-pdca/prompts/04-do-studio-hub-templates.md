# PDCA 04 — Studio hub templates

## Do

`BoardsArtifactsHub.tsx`: template buttons POST `session_template`, navigate to `/studio/board/:id?phase=discover&repo=…`.

## Check

Manual or smoke: create `roadmap_session` returns `board_id`.
