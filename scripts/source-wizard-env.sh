#!/usr/bin/env bash
# Source before `python3 -m lenses` if you start Lenses manually (not via run-lenses.sh / Electron).
# Usage: source scripts/source-wizard-env.sh
export LENSES_EXPERIMENTAL_BLUEPRINTS_WIZARD="${LENSES_EXPERIMENTAL_BLUEPRINTS_WIZARD:-1}"
