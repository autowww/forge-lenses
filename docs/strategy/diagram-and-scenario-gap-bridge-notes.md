# Diagram and scenario coverage (gap-bridge notes)

Audience: **maintainer** — explains how public diagram counts relate to the gap-bridge prompt pack without implying production HTML is incomplete.

## Diagram target (ZIP prompt 06)

The pack suggested roughly **25** public/nav pages carrying Kitchen Sink **`blueprint-diagram*`** fences or documented rationale.

Inventory (`documentation-inventory.json` field `diagram_public_page_hits_approx`) approximates fence hits via substring search. It intentionally ignores maintainer-only Markdown and does not classify diagram quality.

Prompt 06 §2 pages now include explicit diagrams. Remaining nav pages follow a **thin-page policy** until Studio and Wizard URL churn slows, so we do not rewrite visuals on every route-table update.

## Scenario backlog

Suggested scenario Markdown files (enterprise network binding, OIDC login, docs inventory refresh, live deploy parity) ship as **hub-linked backlog rows** until each page can satisfy VERIFY/RECOVERY blocks without private relative links. Canonical pointer: `docs/handbook-public/26-examples-scenarios-hub.md`.

## When to revisit

Refresh this note after a major `/studio/` IA change or when analytics prove readers need more visuals on a specific hub.
