# For generator/build-lenses-docs.py and forge-autodoc tutorial build (dashboard server is stdlib).
markdown>=3.5
PyYAML>=6.0

# Optional: PNG previews for handbook reference pages (docs/index.md links).
# Optional doc/board PNG captures: pip install playwright && playwright install chromium
# playwright>=1.40.0
